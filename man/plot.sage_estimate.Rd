% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.sage_estimate}
\alias{plot.sage_estimate}
\title{Forest Plot of SAGE Estimates}
\usage{
\method{plot}{sage_estimate}(
  x,
  sort = TRUE,
  xlab = expression(hat(Psi)[j]),
  main = "Conditional SAGE with Wald intervals",
  ...
)
}
\arguments{
\item{x}{An object of class \code{sage_estimate}.}

\item{sort}{Logical. Order features by \code{psi_hat} (default \code{TRUE}).}

\item{xlab, main}{Axis label and title.}

\item{...}{Passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}.}
}
\value{
\code{x}, invisibly. Called for the side effect of drawing.
}
\description{
Draws one horizontal confidence interval per feature, ordered by estimated
importance, with a reference line at zero. Features whose interval excludes
zero are drawn in a solid style, the others dashed, so that the outcome of
the test is readable directly off the plot.
}
\examples{
set.seed(1)
X <- matrix(rnorm(300 * 4), 300, 4)
colnames(X) <- paste0("X", 1:4)
f <- function(Xm) 2 * Xm[, 1] - Xm[, 2]
res <- sage_cv_gcop(f, X, K = 2, M = 10, B = 8, seed = 1)
plot(res)

}
