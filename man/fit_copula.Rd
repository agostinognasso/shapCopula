% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copula_fit.R
\name{fit_copula}
\alias{fit_copula}
\title{Fit a Nonparametric Vine Copula with Empirical Marginals}
\usage{
fit_copula(X, family_set = "nonparametric", cores = 1, order = NULL)
}
\arguments{
\item{X}{Numeric matrix or data frame of shape n-by-p.}

\item{family_set}{Character. Copula family set passed to
\code{rvinecopulib::vinecop}. Default \code{"nonparametric"} uses kernel TLL pairs.
Use \code{"parametric"} or a specific family vector for faster fitting.}

\item{cores}{Integer. Number of cores for parallel pair-copula fitting
(passed to \code{rvinecopulib::vinecop}).}

\item{order}{Optional integer vector giving a variable order. When supplied,
the structure is fixed to the D-vine with that order instead of being
selected from the data. This is what makes exact conditional sampling
possible: for a D-vine the Rosenblatt transform conditions sequentially
from the end of the order, so any \emph{suffix} of \code{order} can be conditioned on
exactly. \code{\link[=sage_cv]{sage_cv()}} uses this internally, fitting one D-vine
per Shapley permutation.}
}
\value{
A list with components:
\describe{
\item{\code{vc}}{A \code{vinecop} object from \code{rvinecopulib}.}
\item{\code{ecdfs}}{List of p empirical CDF functions.}
\item{\code{sorted_cols}}{List of p sorted column vectors.}
\item{\code{X_train}}{The original training matrix.}
\item{\code{n_train}}{Number of training rows (used to clip pseudo-observations).}
}
}
\description{
Estimates the joint distribution of \code{X} using a nonparametric vine copula
(transformation local-likelihood, TLL; Geenens 2014) fitted via
\code{rvinecopulib::vinecop}. Marginals are modelled via the empirical CDF.
}
\details{
\strong{Computational note.} Vine copulas scale as O(p^2) pair-copulas. For
nonparametric families this is feasible for p up to approximately 10-15 on
standard hardware. For larger p, use \code{\link[=fit_gauss_copula]{fit_gauss_copula()}} instead.
}
\examples{
\donttest{
set.seed(1)
X <- matrix(rnorm(150 * 3), 150, 3)
vc <- fit_copula(X, family_set = "parametric")
class(vc$vc)  # "vinecop"
}

}
\references{
Geenens, G., Charpentier, A., & Paindaveine, D. (2017).
Probit transformation for nonparametric kernel estimation of the copula
density. \emph{Bernoulli}, 23(3), 1848-1873.
}
\seealso{
\code{\link[=sage_cv]{sage_cv()}} for the vine-copula-based SAGE workflow.
\code{\link[=fit_gauss_copula]{fit_gauss_copula()}} for a faster Gaussian-copula alternative.
}
