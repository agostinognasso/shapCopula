% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sage.R
\name{sage_cv_gcop}
\alias{sage_cv_gcop}
\title{Conditional SAGE via Gaussian Copula with Cross-Fitting}
\usage{
sage_cv_gcop(f_predict, X, K = 3L, M = 30L, B = 20L, alpha = 0.05, seed = NULL)
}
\arguments{
\item{f_predict}{Function mapping an n-by-p numeric matrix to a numeric
vector of predictions of length n.}

\item{X}{Numeric matrix or data frame, shape n-by-p.}

\item{K}{Integer. Number of cross-fitting folds (default 3).}

\item{M}{Integer. Number of random permutations per fold for the
permutation-sampling approximation of the Shapley weights (default 30).}

\item{B}{Integer. Conditional samples per evaluation point per permutation
step (default 20). Internally split into two sub-batches of size \code{B/2}
for U-statistic debiasing of the squared loss.}

\item{alpha}{Numeric. Significance level for Wald confidence intervals
(default 0.05).}

\item{seed}{Optional integer for \code{set.seed()} before fold assignment.}
}
\value{
A \code{data.frame} with one row per feature and columns:
\describe{
\item{\code{feature}}{Feature name (from \code{colnames(X)} or \code{"X1"}, ...).}
\item{\code{psi_hat}}{One-step SAGE estimate \eqn{\hat\Psi_j}.}
\item{\code{se}}{Asymptotic standard error \eqn{\hat\sigma_j / \sqrt{n}}.}
\item{\code{ci_lo}, \code{ci_hi}}{Wald \eqn{(1-\alpha)} confidence interval.}
\item{\code{z}}{Wald z-statistic for \eqn{H_0: \Psi_j = 0}.}
\item{\code{pvalue}}{Two-sided p-value.}
}
}
\description{
Computes global feature importance (SAGE) using the conditional value
function \eqn{v(S; x) = E[f(X) \mid X_S = x_S]} estimated via a Gaussian
copula with empirical marginals. Inference relies on a one-step debiased
estimator with K-fold cross-fitting.
}
\details{
The Gaussian copula is fitted on each training fold via \code{\link[=fit_gauss_copula]{fit_gauss_copula()}}.
Conditional distributions \eqn{X_{-S} \mid X_S = x_S} are sampled
analytically on the latent normal scale, then back-transformed through
empirical marginal quantiles.

\strong{Computational complexity} scales as \eqn{O(K \times M \times p \times n \times B)},
feasible for \eqn{p \lesssim 15} on standard hardware. The Gaussian copula
has no pair-copula fitting overhead, making it substantially faster than
the full vine estimator \code{\link[=sage_cv]{sage_cv()}}.

\strong{Debiasing.} Each squared-loss term
\eqn{\delta_S(x) = (f(x) - v_S(x))^2} is estimated by a U-statistic that
uses two independent sub-batches of size \code{B/2}, removing the
Monte-Carlo-squared noise bias from the plug-in squared estimator.
}
\examples{
\donttest{
set.seed(42)
n <- 400; p <- 5
X <- matrix(rnorm(n * p), n, p)
colnames(X) <- paste0("X", seq_len(p))
# f depends mainly on X1, moderately on X2
f_lin <- function(Xm) 2 * Xm[, 1] - Xm[, 2]
result <- sage_cv_gcop(f_lin, X, K = 3, M = 20, B = 10, seed = 1)
print(result)
}

}
\references{
Gnasso, A. (2026). \emph{Inference for Conditional Shapley Values via Vine Copulas}.
Manuscript under review.

Covert, I., Lundberg, S., & Lee, S.-I. (2020). Understanding global feature
contributions with additive importance measures. \emph{NeurIPS}, 33.
}
\seealso{
\code{\link[=sage_cv]{sage_cv()}} for vine-copula conditional sampling;
\code{\link[=sage_cv_marginal]{sage_cv_marginal()}} for the interventional baseline.
}
