% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sage.R
\name{sage_cv_marginal}
\alias{sage_cv_marginal}
\title{Marginal (Interventional) SAGE with Cross-Fitting}
\usage{
sage_cv_marginal(
  f_predict,
  X,
  K = 3L,
  M = 30L,
  B = 20L,
  alpha = 0.05,
  seed = NULL
)
}
\arguments{
\item{f_predict}{Function mapping an n-by-p numeric matrix to a numeric
vector of predictions of length n.}

\item{X}{Numeric matrix or data frame, shape n-by-p.}

\item{K}{Integer. Number of cross-fitting folds (default 3).}

\item{M}{Integer. Number of random permutations per fold for the
permutation-sampling approximation of the Shapley weights (default 30).}

\item{B}{Integer. Conditional samples per evaluation point per permutation
step (default 20). Internally split into two sub-batches of size \code{B/2}
for U-statistic debiasing of the squared loss.}

\item{alpha}{Numeric. Significance level for Wald confidence intervals
(default 0.05).}

\item{seed}{Optional integer for \code{set.seed()} before fold assignment.}
}
\value{
A \code{data.frame} with the same columns as \code{\link[=sage_cv_gcop]{sage_cv_gcop()}}.
}
\description{
Computes global feature importance using the \strong{marginal} (interventional)
value function \eqn{v_\text{marg}(S; x) = E_{X_{-S}}[f(x_S, X_{-S})]},
where \eqn{X_{-S}} is drawn independently from its marginal empirical
distribution (bootstrap rows of training data), ignoring the conditional
dependence given \eqn{X_S}. This corresponds to the interventional SHAP
framework (Janzing et al. 2020).
}
\details{
This estimator serves as a competitor / baseline for \code{\link[=sage_cv_gcop]{sage_cv_gcop()}}.
When features are independent, both estimators coincide. Under dependence,
the marginal value function breaks the joint distribution and can
misattribute importance to spuriously correlated features.

The marginal sampler is computationally inexpensive (no copula fitting)
and scales to arbitrary p.
}
\examples{
\donttest{
set.seed(42)
n <- 400; p <- 5
X <- matrix(rnorm(n * p), n, p)
colnames(X) <- paste0("X", seq_len(p))
f_lin <- function(Xm) 2 * Xm[, 1] - Xm[, 2]
result <- sage_cv_marginal(f_lin, X, K = 3, M = 20, B = 10, seed = 1)
print(result)
}

}
\references{
Janzing, D., Minorics, L., & Bloebaum, P. (2020). Feature relevance
quantification in explainable AI: A causal problem.
\emph{AISTATS}, 2907-2916.
}
\seealso{
\code{\link[=sage_cv_gcop]{sage_cv_gcop()}} for the conditional (copula-based) estimator.
}
