% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copula_fit.R
\name{fit_gauss_copula}
\alias{fit_gauss_copula}
\title{Fit a Gaussian Copula with Empirical Marginals}
\usage{
fit_gauss_copula(X)
}
\arguments{
\item{X}{Numeric matrix or data frame of shape n-by-p.}
}
\value{
A list with components:
\describe{
\item{\code{Sigma}}{p-by-p latent Gaussian correlation matrix.}
\item{\code{ecdfs}}{List of p empirical CDF functions.}
\item{\code{sorted_cols}}{List of p sorted column vectors (for quantile
back-transformation).}
\item{\code{X_train}}{The original training matrix (stored for use by the
conditional sampler).}
}
}
\description{
Estimates the joint distribution of \code{X} using a Gaussian copula: marginals
are modelled non-parametrically via the empirical CDF, and the copula
dependence structure is captured by the correlation matrix of the
normal-quantile-transformed pseudo-observations.
}
\details{
Pseudo-observations are computed as \code{u_j = rank(X_j) / (n + 1)} to avoid
boundary values. The latent correlation matrix is the sample correlation of
\code{qnorm(U)}. Eigenvalue flooring at \code{1e-8} ensures positive definiteness.
}
\examples{
set.seed(1)
X <- matrix(rnorm(200 * 4), 200, 4)
gc <- fit_gauss_copula(X)
dim(gc$Sigma)  # 4 x 4

}
\references{
Gnasso, A. (2026). \emph{Inference for Conditional Shapley Values via Vine Copulas}.
Manuscript under review.
}
\seealso{
\code{\link[=sage_cv_gcop]{sage_cv_gcop()}} for the main estimation workflow.
}
